apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: dmb-namespace
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  generators:
    - list:
        elements:
          - env: eu-dev
            namespace: dmb-dev
          - env: eu-staging
            namespace: dmb-staging
          - env: eu-prod
            namespace: dmb-prod
  template:
    metadata:
      name: 'dmb-namespace-{{env}}'
      annotations:
        argocd.argoproj.io/sync-wave: "0"
    spec:
      project: default
      sources:
        - repoURL: https://github.com/ravikrs/dmb-gitops1
          path: charts/dmb-namespace
          targetRevision: HEAD
          helm:
            valueFiles:
              - $values/teams/dmb/{{env}}/dmb-namespace-values.yaml
        - repoURL: https://github.com/ravikrs/dmb-gitops1
          targetRevision: HEAD
          ref: values
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{namespace}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=false
