# dmb-gitops1

GitOps repository for the DMB dev team. Contains Helm charts and ArgoCD ApplicationSets to deploy DMB services across `dmb-dev`, `dmb-staging`, and `dmb-prod` namespaces.

## Quick Start

See [Architecture](01-architecture.md) for the full structure.

To serve these docs locally:

```bash
pip install mkdocs-material
mkdocs serve
```
